# Blender Computer-Use Isolated VM & MCP Server v1.0.0

Initial production release featuring:
- **Isolated Hyper-V VM with GPU-PV:** NVIDIA RTX 4080 Super hardware acceleration with zero host mouse/focus disturbance.
- **Blender 5.2.0 LTS Integration:** Staged release build with in-process Python telemetry bridge on port 9199.
- **14 Model Context Protocol (MCP) Tools:** Full suite for synthetic mouse/drag/click, keyboard, 1080p DXGI capture, UI Automation tree inspection, and sub-2s golden snapshot rollbacks.
- **One-Click Master Installer:** Automated setup scripts (`install.bat`, `install.ps1`, `install.py`, `guest/install_guest.ps1`).
- **Claude Code & OMP Plugin:** Verified plugin manifest and managed agent skill (`blender-computer-use-vm`).
